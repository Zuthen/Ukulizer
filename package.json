{
  "type": "module",
  "scripts": {
    "test": "jest --env=jsdom",
    "dev": "live-server"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "live-server": "^1.2.1"
  },
    "jest": {
      "verbose": true,
      "testEnvironment": "jsdom"
    }
  }

  
